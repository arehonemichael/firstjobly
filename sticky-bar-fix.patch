diff --git a/components/StickyApplyBar.jsx b/components/StickyApplyBar.jsx
index 2124bfe..e314f96 100644
--- a/components/StickyApplyBar.jsx
+++ b/components/StickyApplyBar.jsx
@@ -12,13 +12,16 @@ export default function StickyApplyBar({ title, company, link, slug }) {
   if (!link) return null;
   return (
     <div
-      className={`fixed bottom-0 left-0 right-0 z-50 bg-white border-t border-gray-200 shadow-lg px-4 py-3 flex items-center justify-between gap-4 transition-transform duration-300 ${
+      className={`fixed bottom-0 left-0 right-0 z-[2147483647] bg-white border-t border-gray-200 shadow-lg px-4 py-3 flex items-center justify-between gap-4 transition-transform duration-300 ${
         visible ? "translate-y-0" : "translate-y-full"
       }`}
     >
-      <div className="min-w-0">
-        <p className="text-sm font-semibold text-gray-900 truncate">{title}</p>
-        <p className="text-xs text-gray-500 truncate">{company || "Confidential"}</p>
+      <div className="min-w-0 flex items-center gap-2">
+        <span className="text-[10px] font-bold text-pink-600 uppercase tracking-wide flex-shrink-0">FJ</span>
+        <div className="min-w-0">
+          <p className="text-sm font-semibold text-gray-900 truncate">{title}</p>
+          <p className="text-xs text-gray-500 truncate">{company || "Confidential"}</p>
+        </div>
       </div>
       <ApplyButton
         link={link}
